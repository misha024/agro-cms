version: '3.8'

services:
  web:
    build:
      context: ../..
      dockerfile: deployment/dev/Dockerfile.dev
    volumes:
      - ./../..:/app
    ports:
      - "8000:8000"
    environment:
      - DJANGO_SETTINGS_MODULE>=main.settings.dev